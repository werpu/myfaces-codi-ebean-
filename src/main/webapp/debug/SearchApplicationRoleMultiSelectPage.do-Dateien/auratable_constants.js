var AuraTableConstants = {
	selectionClassName: 'click',
	
	/* 
	 * Class to be put onto the checkbox which performs row selection.
	 * The rows in the table corresponding to checked checkboxes will be
	 * highlighted when the table is first displayed.
	 */
	rowSelectionControlClassName: 'rowSelectionControl'
};
